# mbaelde.github.io

Site perso de Maxime Baelde (Hugo). Backlinks vers MB Prod Studio et Duetly gérés dans `hugo.toml` (`params.description`) et `content/_index.md`. Publications dans `data/publications.json`.

## Conventions Git

**Exception à la règle globale IPCRA** (commit + push habituellement proposés, jamais exécutés directement) : sur ce repo, commit et push directs sont autorisés sans validation préalable à chaque fois.
